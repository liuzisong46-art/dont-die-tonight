export type Stats={battery:number;energy:number;money:number;alertness:number;danger:number;distance:number;time:number;flags:string[]};export type Effects=Partial<Omit<Stats,'flags'|'time'>>&{time?:number;flag?:string};export type Choice={label:string;nextNode:string;effects?:Effects;condition?:(s:Stats)=>boolean};export type Node={id:string;text:string[];choices:Choice[];ending?:'safe'|'phone'|'wallet'|'lost'|'danger'|'local'};
