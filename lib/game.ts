import {Effects,Stats} from '@/types/game';export const initial:Stats={battery:11,energy:70,money:80,alertness:50,danger:10,distance:3.2,time:107,flags:[]};export const clock=(m:number)=>`${String(Math.floor(m/60)).padStart(2,'0')}:${String(m%60).padStart(2,'0')}`;export function apply(s:Stats,e:Effects={}){const{flag,...v}=e,n={...s};Object.entries(v).forEach(([k,x])=>{if(typeof x==='number')(n as unknown as Record<string,number>)[k]+=x});if(flag&&!n.flags.includes(flag))n.flags=[...n.flags,flag];n.battery=Math.max(0,n.battery);n.money=Math.max(0,n.money);n.distance=Math.max(0,+n.distance.toFixed(1));return n}export function grades(s:Stats){return[['生存能力',s.danger<25?'S':s.danger<45?'A':'C'],['警觉性',s.alertness>=65?'S':s.alertness>=45?'A':'C'],['作死指数',s.danger<20?'S':s.danger<45?'A':'D'],['运气',s.money>30?'A':s.money>0?'B':'C']]}
